services:
  db_test:
    image: postgres:16.3-alpine3.20
    restart: always
    ports:
      - '3556:5432'
    container_name: bapp-db-test
    environment:
      POSTGRES_USER: postgres_test
      POSTGRES_PASSWORD: postgres_test
      POSTGRES_DB: bapp_test